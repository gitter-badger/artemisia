{
  foo = bar
  hello = world


  __setting__ = {

    dag = {
      attempts = 1,
      concurrency = 1,
      cooldown= 100ms,
      heartbeat_cycle= 1000ms,
      disable_assertions = false,
      ignore_conditions = false,
    }

    core = {
      working_dir = /var/tmp/ultron
    }

    logging = {
      console_trace_level= DEBUG,
      file_trace_level= DEBUG
    }

    components = {
      TestComponent = tech.artemesia.task.TestComponent
    }

  }
}